import { Typography } from '@mui/material';

const Title = () => (
  <>
    <Typography
      variant='h2'
      sx={{
        textAlign: 'center'
      }}>
      React Todo App
    </Typography>
    <Typography
      variant='h5'
      sx={{
        textAlign: 'center'
      }}>
      with MUI
    </Typography>
  </>
);
export default Title;
